"""External-service integrations.

Google account integration (Gmail / Drive / Calendar) is deliberately OUT OF
SCOPE for this build and is stubbed in ``google.py`` to raise
``NotImplementedError``. Real OAuth wiring happens in a later, manually
reviewed phase.
"""
